language: java
jdk: openjdk11

jobs:
    include:
        - stage: build
          script: ./gradlew clean build
        - stage: test
          script: ./gradlew test
          

stages:
    - build
    - test

