language: java
jdk: openjdk11

jobs:
    include:
        - stage: build
          script: ./gradlew clean assemble
        - stage: test
          script: ./gradlew check
          

stages:
    - build
    - test

